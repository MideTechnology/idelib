# -*- mode: python -*-
a = Analysis(['filesplit.py'],
             pathex=['..', 'C:\\Users\\dstokes\\workspace\\SSXViewer\\splitter'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='filesplit.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='filesplit')
